$ php -i | grep "Loaded Configuration File"
  └─>Loaded Configuration File => C:\php-8.5.7-Win32-vs17-x64\php.ini
  php --ini ??

# Global Package Location
npm root -g                   # Find global npm package directory

nano => Ctrl + W = search

$ php -m | grep sqlite (If it returns pdo_sqlite, you are done!)
  └─>pdo_sqlite

$ composer create-project laravel/laravel planpacer-3.13

$ composer require laravel/fortify
  └─>Install Fortify via Composer

$ php artisan fortify:install
  └─>Inject Fortify’s configuration file, migration files, and actions into the app directory

$ php artisan migrate
  └─>??

$ php artisan vendor:publish --provider="Laravel\Fortify\FortifyServiceProvider"
  └─>After this, check database/migrations/ for new files

$ php artisan vendor:publish --tag=fortify-config
  └─>After this, edit config/fortify.php as needed

$ mkdir -p resources/views/auth

$ php artisan vendor:publish --list
  └─>Shows everything you can publish from all packages

$ php artisan route:list
  └─>see all routes (including Fortify's)

$ php artisan cache:clear && php artisan route:clear && php artisan view:clear && php artisan config:clear

$ php artisan about
